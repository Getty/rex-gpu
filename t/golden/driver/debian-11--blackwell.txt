# golden: driver/debian-11--blackwell
# Host interactions Rex::GPU emits, in order. Regenerate with
# REX_GPU_GOLDEN_UPDATE=1 prove -l t/ -- and read the diff.
run: nvidia-smi -L 2>&1
run(auto_die=default): uname -r
run: dpkg --print-architecture
DIE: Blackwell-class NVIDIA GPU on Debian release '11.11': Debian's own nvidia packages cannot drive it and NVIDIA's CUDA repo only covers Debian 12 and 13 — install the driver manually
